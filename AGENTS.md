# Workspore

Workspore：把调校好的 AI 工作区固化为带版本的模板，让新工作区一落地就是调校好的状态。术语表见 CONTEXT.md，产品定义见 Workspore产品定义.md。

## Agent skills

### Issue tracker

本地 markdown：issues 与 specs 存放在 `.scratch/<feature>/` 下。见 `docs/agents/issue-tracker.md`。

### Triage labels

照用默认五个分拣标签（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录一个 CONTEXT.md + docs/adr/。见 `docs/agents/domain.md`。
