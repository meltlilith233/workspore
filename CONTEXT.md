# Workspore

把调校好的 AI 工作区固化为带版本的模板，让新工作区一落地就是调校好的状态。本文件是项目的唯一术语表。

## Language

**模板**:
从调校好的工作区固化出来的、带版本的可复用资产，由上下文、能力与作者显式选入的文件构成；新工作区由它落地。
_Avoid_: 预设

**实例**:
由模板落地而成的工作区。

**上下文**:
指令文件（AGENTS.md 等）承载的约定：目录怎么摆、文件怎么命名、任务怎么流转、agent 的行为规范。

**能力**:
让 agent 会干这类任务的东西：skills、命令、MCP 声明。

**家底**:
每次任务自己的东西：素材、产出、密钥、本机环境。永不进模板。

**出身**:
create 时写入实例的一行记录：来源模板与版本。
