# 指令文件有哪个存哪个，不做归一

多工具共用一个工作区时，各家认的指令文件不同（AGENTS.md、CLAUDE.md 等）。我们决定模板原样存储源工作区里存在的所有指令文件，不归一成单一中立源。理由：所见即所得——源工作区怎么调校的，模板就怎么存、实例就怎么还原；归一（单一源 + 按工具生成变体）会引入「生成器」这层间接和复杂度。

**Consequences**: 同一处约定若在多份指令文件中重复出现，改进回流时需要人裁决写回哪几份；这是用保真和简单换来的代价，由 save / update 环节承担。
