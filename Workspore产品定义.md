# Workspore 产品定义

## 产品目标

**让 AI 工作区开箱即用**：把调校好的工作区固化为带版本的模板，让下一个工作区一落地就是调校好的状态，不再从零搭建脚手架。

验收标准：新工作区里，对 agent 下达一句该工作区的典型任务指令，无需人工补充任何脚手架上下文，agent 即可按规范完成任务（密钥、鉴权等服务的开通配置不属于脚手架上下文）。这是量模板质量的尺子——首跑要不要引导、怎么引导，是模板作者写进上下文的普通内容，不是产品功能。

核心用户：频繁开新类型任务（视频创作、深度调研……）、被重复搭脚手架磨损的重度 AI 工具用户。

## 模板装什么

一个 AI 工作区拆开是三样东西：**上下文**（指令文件承载的约定：目录怎么摆、文件怎么命名、任务怎么流转、agent 的行为规范）、**能力**（skills、命令、MCP 声明）、**家底**（素材、产出、密钥、本机环境）。被磨损的人反复打磨的是前两样。

模板 = 固化的**上下文 + 能力**，外加作者显式选入的文件（脚本、范本、prompt 库——默认不进，选了才进）。目录骨架一个不带：结构写在上下文里，新实例落地时 agent 读了上下文自己长出来。搬的不是目录，是让 agent 自己长出目录的东西——这是和传统脚手架工具的分界线。

## 工具形态

双向 CLI，两个动作，**初始化之后退场**：

- **save**——把调校好的工作区固化成模板。固定的规则逻辑：上下文、能力、选入清单，脱敏打包。Git 仓库即模板，版本就是 tag（save 一次多一个 tag，默认递增，大版本手动标）。
- **create**——从模板落出新工作区，落地即巅峰；顺手留一行**出身**（来自哪个模板哪个版本）。

初始化之后，工作区就是工作区自己的事：旧工作区想跟上模板新版本，交给它里面坐着的 agent 照着模板仓库自己干。分工就一条：**工具管字节（固化、落地），agent 管语义（整理、合并、引导）**。分享就是公开仓库，公开即分享，任何人 clone 即用；平台（市场、目录、收录）不做。

## 用户故事

**1 开新任务**——我要开一个新类型的任务，过去每次都得重搭目录结构、命名规范、流转约定、常用 skills，摩擦大到吃掉开工意愿，或者草草开工、没用上以前调校好的家伙。现在新工作区落地即巅峰状态，我的时间只花在任务本身。

**2 成果沉淀**——我在某个工作区打磨出的约定和工作流，过去随任务结束散落在一次性目录里，下次开同类任务全靠回忆和手抄。现在它们沉淀为带版本的模板资产，被每一次使用持续打磨，越用越准。

**3 改进传播**——模板有了改进，过去好东西困死在单个实例里，下次开同类任务还得从头再来。现在改进沉淀进模板，**以后开的每一个工作区都免费拿到**；已开出去的旧工作区想跟上，交给它里面坐着的 agent 照着模板仓库自己干——工具不承诺这个，agent 本来就会。

**4 跨机复用**——换电脑、重装系统时，过去要花几个小时重配，还总会漏掉当年踩坑才补上的那几条设置。现在模板本身就是备份，任何新机器都能还原出同样的工作环境。

**5 分享与协作**——朋友和协作者想要同款工作区，过去只能整个目录连素材带密钥一起拷，又乱又不安全。现在分享出去的是干净的配置与约定，素材和密钥留在各自本机，对方完成自己的一次性密钥配置后即可开箱使用。模板就是 Git 仓库，设为公开，陌生人也能开箱使用；平台（市场、收录）不做。

痛点一句话：脚手架是每个 AI 重度用户交的隐形税——反复交、难沉淀、带不走。Workspore 让它只交一次。

## 记录待议

- **save 接入 LLM**（2026-09-17 讨论存档）：让 agent 参与 save——按「换个新任务还用不用」提议选入文件；做规则引擎之上的第二道语义脱敏网（规则只抓已知套路，模型能拦 prompt 里手滑粘进去的密钥）；把散碎改动整理成版本说明。**先不接**：默认规则模式保持确定、可预测；安全环节不交给概率机器。重估时机：等显式选入的配置被证明太繁琐。
